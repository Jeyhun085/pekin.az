<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Catalog</title>
</head>
<% if (item.stock > 0) { stock = "Anbarda var" }
else if (item.inTransit > 0) {  stock = "Yolda var" }
else {  stock = "Bitib" } %> 

<body>
   <h1>Price: <%= item.salePrices[0].value / 100 + " AZN" %> </h1>
   <h1>Article: <%= item.article %></h1>
   <h1>Name: <%= item.name %></h1>
   <h1>Stock: <%= stock%></h1>
   <div class="input-group mb-3">
    <input id="input_<%= item.article %>" type="text" class="form-control" placeholder="Miqdar"
      aria-label="Recipient's username" aria-describedby="basic-addon2">
    <div class="input-group-append">
      <button id="<%= item.article %>" class="btn btn-outline-secondary" type="button"
        onClick="addItemToCart( document, this.id);">Buy</button>
    </div>
  </div>
   <script src="functions.js"></script>
</body>

</html>